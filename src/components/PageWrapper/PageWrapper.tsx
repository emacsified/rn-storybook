import React from "react";
import { SafeAreaView } from "react-native";

const PageWrapper: React.FC = ({ children }): React.ReactElement => <SafeAreaView>{children}</SafeAreaView>;

export default PageWrapper;
